from tools import _set_alarm
import sys

time_str = "every tuesday at 1:30 am"
label = "JARVIS Alarm"

# Just run it to see what _set_alarm does
# Actually, since I don't want to mess up the user's alarms, let's just write a wrapper
