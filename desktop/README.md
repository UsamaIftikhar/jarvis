# Tauri Desktop Shell

Implemented in **Phase 6**. We'll run `pnpm create tauri-app@latest` here pointed
at the `frontend/` Next.js export, then add a sidecar config that boots the
FastAPI backend on app start.
